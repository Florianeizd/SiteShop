{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
<section class="articles">

    {% for article in articles %}
        <article>
            <h2>{{ article.nom }}</h2>
            <div class="metadata">Publié le 30/11/2021 à 11:31 dans la catégorie Pull</div>
            <div class="content">
                <img src="http://placehold.it/350x150" alt=""><br>
                {{ article.prix }}
                {{ article.description | raw }}
                <a href="{{ path('article_show', {'id' : article.id }) }}" class=" btn btn-primary">Voir plus</a>
            </div>
        </article>
    {% endfor %}
   

</section>

{% endblock %}
